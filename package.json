{
  "name": "soniq",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "generate-schema": "tsx tools/generate-json-schema.ts"
  },
  "devDependencies": {
    "tsx": "^4.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.4.0",
    "zod-to-json-schema": "^3.25.2"
  },
  "packageManager": "pnpm@9.0.0"
}
