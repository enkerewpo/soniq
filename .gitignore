node_modules/
dist/
# shared/dist/ is intentionally committed (consumed by Node for Max via the
# JSON Schema artifact + by the MCP server at runtime via pnpm workspace link).
!shared/dist/
!shared/dist/**
build/
*.log
.DS_Store
.env
.env.local
*.tsbuildinfo

# Max for Live build artifacts
*.maxsnap
Max.log

# M4L device binary: gitignored during Plan 1 iteration; will be added once stable.
Soniq.Bridge/*.amxd

# Max-side scratch files
Soniq.Bridge/.amxd.tmp
Soniq.Bridge/.maxpat.tmp

# NOTE: Soniq.Bridge.maxproj IS committed — it's the Max Project descriptor and
# is necessary for clone-and-go. Max will edit local-only fields (viewrect,
# timestamps) on each save; treat those diffs as noise.
